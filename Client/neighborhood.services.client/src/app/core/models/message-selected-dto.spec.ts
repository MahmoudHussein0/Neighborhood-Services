import { MessageSelectedDto } from './message-selected-dto';

describe('MessageSelectedDto', () => {
  it('should create an instance', () => {
    expect(new MessageSelectedDto()).toBeTruthy();
  });
});
